import React, { useState } from 'react';
import CardsTextWrapper from './CardsText.style'; // Import the CSS file
import Tooltip from '../tooltip';

const CardsText = ({ title, value, token, usd }) => {

    
    return (
        <CardsTextWrapper>
            
        </CardsTextWrapper>
    );
};

export default CardsText;